{
  "version": 2,
  "buildCommand": "npm run build --workspace=apps/bet",
  "outputDirectory": "apps/bet/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
